# double precision triangular matrix-vector product
Signature(
    "dtrmv_",
    Uplo(),
    Trans(),
    Diag(),
    Dim("n"),
    dData("A", "ldA * n", "lower if uplo == 'L' eles upper"),
    Ld("ldA", "n"),
    dData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    complexity = "n ** 2 + n"  # checked
)

# double precision vector-vector outer product
Signature(
    "dger_",
    Dim("m"),
    Dim("n"),
    dScalar(),
    dData("X", "1 + (m - 1) * abs(incX)"),
    Inc("IncX"),
    dData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("IncY"),
    dData("A", "ldA * n"),
    Ld("LdA", "m"),
    complexity = "2 * m * n"
)

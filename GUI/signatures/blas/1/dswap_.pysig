# double precision vector swap
Signature(
    "dswap_",
    Dim("n"),
    dData("X", "1 + (n - 1) * abs(incX)"),
    Inc("IncX"),
    dData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("IncY"),
    complexity = "0"  # only memops
)

# double precision LU factorization with pivoting (unblocked)
Signature(
    "dgetf2_",
    Dim("m"),
    Dim("n"),
    dData("A", "ldA * n"),
    Ld("ldA", "m"),
    iData("ipiv", "min(m, n)"),
    Info(),
    complexity="n * (n + 1) * (2 * n + 1) / 3"
)

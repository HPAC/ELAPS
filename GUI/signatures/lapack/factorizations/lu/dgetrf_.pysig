# double precision LU factorization with pivoting
Signature(
    "dgetrf_",
    Dim("m"),
    Dim("n"),
    dScalar(),
    dData("A", "ldA * n"),
    Ld("ldA", "m"),
    iData("ipiv", "min(m, n)"),
    Info()
)

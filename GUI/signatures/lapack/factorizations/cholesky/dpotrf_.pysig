# double precision cholesky factorization
Signature(
    "dpotrf_",
    Uplo(),
    Dim("n"),
    dScalar(),
    dData("A", "ldA * n", "lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    Info()
)

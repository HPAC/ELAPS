(('slacn2', 'SLACN2 estimates the 1-norm of a square, real matrix A.\nReverse communication is used for evaluating matrix-vector products.'), ('N', 'N is INTEGER\nhe order of the matrix.  N >= 1.'), ('V', 'V is REAL array, dimension (N)\nn the final return, V = A*W,  where  EST = norm(V)/norm(W)\nW is not returned).'), ('X', 'X is REAL array, dimension (N)\nn an intermediate return, X should be overwritten by\n     A * X,   if KASE=1,\n     A**T * X,  if KASE=2,\nnd SLACN2 must be re-called with all the other parameters\nnchanged.'), ('ISGN', 'ISGN is INTEGER array, dimension (N)'), ('EST', 'EST is REAL\nn entry with KASE = 1 or 2 and ISAVE(1) = 3, EST should be\nnchanged from the previous call to SLACN2.\nn exit, EST is an estimate (a lower bound) for norm(A). '), ('KASE', 'KASE is INTEGER\nn the initial call to SLACN2, KASE should be 0.\nn an intermediate return, KASE will be 1 or 2, indicating\nhether X should be overwritten by A * X  or A**T * X.\nn the final return from SLACN2, KASE will again be 0.'), ('ISAVE', 'ISAVE is INTEGER array, dimension (3)\nSAVE is used to save variables between calls to SLACN2'))

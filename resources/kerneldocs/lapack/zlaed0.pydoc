(('zlaed0', 'Using the divide and conquer method, ZLAED0 computes all eigenvalues\nof a symmetric tridiagonal matrix which is one diagonal block of\nthose from reducing a dense or band Hermitian matrix and\ncorresponding eigenvectors of the dense or band matrix.'), ('QSIZ', 'QSIZ is INTEGER\nhe dimension of the unitary matrix used to reduce\nhe full matrix to tridiagonal form.  QSIZ >= N if ICOMPQ = 1.'), ('N', 'N is INTEGER\nhe dimension of the symmetric tridiagonal matrix.  N >= 0.'), ('D', 'D is DOUBLE PRECISION array, dimension (N)\nn entry, the diagonal elements of the tridiagonal matrix.\nn exit, the eigenvalues in ascending order.'), ('E', 'E is DOUBLE PRECISION array, dimension (N-1)\nn entry, the off-diagonal elements of the tridiagonal matrix.\nn exit, E has been destroyed.'), ('Q', 'Q is COMPLEX*16 array, dimension (LDQ,N)\nn entry, Q must contain an QSIZ x N matrix whose columns\nnitarily orthonormal. It is a part of the unitary matrix\nhat reduces the full dense Hermitian matrix to a\nreducible) symmetric tridiagonal matrix.'), ('LDQ', 'LDQ is INTEGER\nhe leading dimension of the array Q.  LDQ >= max(1,N).'), ('IWORK', 'IWORK is INTEGER array,\nhe dimension of IWORK must be at least\n            6 + 6*N + 5*N*lg N\n            ( lg( N ) = smallest integer k\n                        such that 2^k >= N )'), ('RWORK', 'RWORK is DOUBLE PRECISION array,\n                     dimension (1 + 3*N + 2*N*lg N + 3*N**2)\n              ( lg( N ) = smallest integer k\n                          such that 2^k >= N )'), ('QSTORE', 'QSTORE is COMPLEX*16 array, dimension (LDQS, N)\nsed to store parts of\nhe eigenvector matrix when the updating matrix multiplies\nake place.'), ('LDQS', 'LDQS is INTEGER\nhe leading dimension of the array QSTORE.\nDQS >= max(1,N).'), ('INFO', 'INFO is INTEGER\n= 0:  successful exit.\n< 0:  if INFO = -i, the i-th argument had an illegal value.\n> 0:  The algorithm failed to compute an eigenvalue while\n      working on the submatrix lying in rows and columns\n      INFO/(N+1) through mod(INFO,N+1).'))

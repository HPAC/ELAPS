(('slaed2', 'SLAED2 merges the two sets of eigenvalues together into a single\nsorted set.  Then it tries to deflate the size of the problem.\nThere are two ways in which deflation can occur:  when two or more\neigenvalues are close together or if there is a tiny entry in the\nZ vector.  For each such occurrence the order of the related secular\nequation problem is reduced by one.'), ('K', 'K is INTEGER\nhe number of non-deflated eigenvalues, and the order of the\nelated secular equation. 0 <= K <=N.'), ('N', 'N is INTEGER\nhe dimension of the symmetric tridiagonal matrix.  N >= 0.'), ('N1', 'N1 is INTEGER\nhe location of the last eigenvalue in the leading sub-matrix.\nin(1,N) <= N1 <= N/2.'), ('D', 'D is REAL array, dimension (N)\nn entry, D contains the eigenvalues of the two submatrices to\ne combined.\nn exit, D contains the trailing (N-K) updated eigenvalues\nthose which were deflated) sorted into increasing order.'), ('Q', 'Q is REAL array, dimension (LDQ, N)\nn entry, Q contains the eigenvectors of two submatrices in\nhe two square blocks with corners at (1,1), (N1,N1)\nnd (N1+1, N1+1), (N,N).\nn exit, Q contains the trailing (N-K) updated eigenvectors\nthose which were deflated) in its last N-K columns.'), ('LDQ', 'LDQ is INTEGER\nhe leading dimension of the array Q.  LDQ >= max(1,N).'), ('INDXQ', 'INDXQ is INTEGER array, dimension (N)\nhe permutation which separately sorts the two sub-problems\nn D into ascending order.  Note that elements in the second\nalf of this permutation must first have N1 added to their\nalues. Destroyed on exit.'), ('RHO', 'RHO is REAL\nn entry, the off-diagonal element associated with the rank-1\nut which originally split the two submatrices which are now\neing recombined.\nn exit, RHO has been modified to the value required by\nLAED3.'), ('Z', 'Z is REAL array, dimension (N)\nn entry, Z contains the updating vector (the last\now of the first sub-eigenvector matrix and the first row of\nhe second sub-eigenvector matrix).\nn exit, the contents of Z have been destroyed by the updating\nrocess.'), ('DLAMDA', 'DLAMDA is REAL array, dimension (N)\n copy of the first K eigenvalues which will be used by\nLAED3 to form the secular equation.'), ('W', 'W is REAL array, dimension (N)\nhe first k values of the final deflation-altered z-vector\nhich will be passed to SLAED3.'), ('Q2', 'Q2 is REAL array, dimension (N1**2+(N-N1)**2)\n copy of the first K eigenvectors which will be used by\nLAED3 in a matrix multiply (SGEMM) to solve for the new\nigenvectors.'), ('INDX', 'INDX is INTEGER array, dimension (N)\nhe permutation used to sort the contents of DLAMDA into\nscending order.'), ('INDXC', 'INDXC is INTEGER array, dimension (N)\nhe permutation used to arrange the columns of the deflated\n matrix into three groups:  the first group contains non-zero\nlements only at and above N1, the second contains\non-zero elements only below N1, and the third is dense.'), ('INDXP', 'INDXP is INTEGER array, dimension (N)\nhe permutation used to place deflated values of D at the end\nf the array.  INDXP(1:K) points to the nondeflated D-values\nnd INDXP(K+1:N) points to the deflated eigenvalues.'), ('COLTYP', 'COLTYP is INTEGER array, dimension (N)\nuring execution, a label which will indicate which of the\nollowing types a column in the Q2 matrix is:\n : non-zero in the upper half only;\n : dense;\n : non-zero in the lower half only;\n : deflated.\nn exit, COLTYP(i) is the number of columns of type i,\nor i=1 to 4 only.'), ('INFO', 'INFO is INTEGER\n= 0:  successful exit.\n< 0:  if INFO = -i, the i-th argument had an illegal value.'))

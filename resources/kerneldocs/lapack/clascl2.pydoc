(('clascl2', 'CLASCL2 performs a diagonal scaling on a vector:\n  x <-- D * x\nwhere the diagonal REAL matrix D is stored as a vector.\n\nEventually to be replaced by BLAS_cge_diag_scale in the new BLAS\nstandard.'), ('M', 'M is INTEGER\number of rows of D and X. M >= 0.'), ('N', 'N is INTEGER\number of columns of D and X. N >= 0.'), ('D', 'D is REAL array, length M\nnal matrix D, stored as a vector of length M.'), ('X', 'X is COMPLEX array, dimension (LDX,N)\ntry, the vector X to be scaled by D.\nit, the scaled vector.'), ('LDX', 'LDX is INTEGER\neading dimension of the vector X. LDX >= 0.'))

# single precision Cholesky factorization
Signature(
    "spotrf",
    Uplo(),
    Dim("n"),
    sData("A", "ldA * n", "lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    Info(),
    complexity="n * (n + 1) * (2 * n + 1) / 6"
)

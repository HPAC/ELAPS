# double precision complex Hermitian generalized reduction to standard
Signature(
    "zhegst",
    Flag("itype", [1, 2, 3]),
    Uplo(),
    Dim("n"),
    zData("A", "ldA * n", "symm, lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    zData("B", "ldB * n", "symm, lower if uplo == 'L' else upper"),
    Ld("ldB", "n"),
    Info()
)

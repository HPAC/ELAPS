# double precision symmetric triangular matrix squaring (unblocked)
Signature(
    "dlauu2",
    Uplo(),
    Dim("n"),
    dData("A", "ldA * n", "lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    Info(),
    flops="n * (n + 1) * (2 * n + 1) / 6"
)

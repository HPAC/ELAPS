# single precision complex Cholesky factorization
Signature(
    "cpotrf",
    Uplo(),
    Dim("n"),
    cData("A", "ldA * n", "lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    Info(),
    complexity="4 * n * (n + 1) * (2 * n + 1) / 6"
)

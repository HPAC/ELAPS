# single precision complex Hermitian generalized reduction to standard
Signature(
    "chegst",
    Flag("itype", [1, 2, 3]),
    Uplo(),
    Dim("n"),
    cData("A", "ldA * n", "symm, lower if uplo == 'L' else upper"),
    Ld("ldA", "n"),
    cData("B", "ldB * n", "symm, lower if uplo == 'L' else upper"),
    Ld("ldB", "n"),
    Info()
)

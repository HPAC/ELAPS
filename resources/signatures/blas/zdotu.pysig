# double precision complex vector-vector inner product
Signature(
    "zdotu",
    Dim("n"),
    zData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    zData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    flops="4 * 2 * n"  # checked
)

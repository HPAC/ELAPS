# double precision complex vector-vector conjugate inner product
Signature(
    "zdotc",
    Dim("n"),
    zData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    zData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    flops="4 * 2 * n"  # checked
)

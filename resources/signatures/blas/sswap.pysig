# single precision vector swap
Signature(
    "sswap",
    Dim("n"),
    sData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    sData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    flops="0"  # only memops
)

# single precision vector copy
Signature(
    "scopy",
    Dim("n"),
    sData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    sData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    complexity = "0"  # only memops
)

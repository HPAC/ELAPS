# double precision vector copy
Signature(
    "dcopy",
    Dim("n"),
    dData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    dData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    flops="0"  # only memops
)

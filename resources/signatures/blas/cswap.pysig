# single precision complex vector swap
Signature(
    "cswap",
    Dim("n"),
    cData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    cData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    flops="0"  # only memops
)

# double precision scaled vector addition
Signature(
    "daxpy",
    Dim("n"),
    dScalar(),
    dData("X", "1 + (n - 1) * abs(incX)"),
    Inc("incX"),
    dData("Y", "1 + (n - 1) * abs(incY)"),
    Inc("incY"),
    complexity = "2 * n"
)

# double precision randomize SPD
Signature(
    "dporand",
    Uplo(),
    Dim("n"),
    dData("A", "ldA * n",
          "lower if uplo == 'L' else upper, symm"),
    Ld("ldA", "n"),
    flops="n * n + n"
)

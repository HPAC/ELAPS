# single precision randomize SPD
Signature(
    "sporand",
    Uplo(),
    Dim("n"),
    sData("A", "ldA * n",
          "lower if uplo == 'L' else upper, symm"),
    Ld("ldA", "n"),
    flops="n * n + n"
)

# double precision complex randomize HPD
Signature(
    "zporand",
    Uplo(),
    Dim("n"),
    dData("A", "ldA * n",
          "lower if uplo == 'L' else upper, symm"),
    Ld("ldA", "n"),
    complexity="n * n + n"
)
